# Runtime dependencies for this repo.
# numpy<2 avoids ABI conflicts with some prebuilt wheels (sklearn/matplotlib) in older envs.
numpy<2
scikit-learn>=1.3
matplotlib>=3.8
transformers>=4.40,<5
torch>=2.6
